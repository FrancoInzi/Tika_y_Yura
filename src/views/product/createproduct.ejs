<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head',{css:"createproduct", title:"Crear Producto"}) %>
    <body>
        <%- include('../partials/header') %>
        <main class="main-createproduct">
            <div class="container-createproduct">
                <div class="form">
                    <form name="formulario" id ="form" method="POST" action="/product/createproduct" enctype="multipart/form-data">
                        <h1 class="h1-crear">Crear un producto</h1>
                        <div>
                            <label class="label-product" for="name"><b>Nombre del producto (4 to 60 characters):</b></label>
                        </div>
                        <div>
                            <input type="text" id="name" name="name" class="form-control <%= locals.errors && errors.name ? 'is-invalid' : null %>"
                                value="<%= locals.oldData ? oldData.name : null %>" autocomplete="off">
                            <% if (locals.errors && errors.name) { %>
                                <div class="text-danger">
                                    <%= errors.name.msg %>
                                </div>
                            <% } %>
                        </div>

                        <div>
                            <label class="label-product" for="other_name"><b>Nombre científico:</b></label>
                        </div>
                        <div>
                            <input type="text" id="other_name" name="other_name" class="form-control <%= locals.errors && errors.review ? 'is-invalid' : null %>"
                                value="<%= locals.oldData ? oldData.other_name : null %>">
                            <% if (locals.errors && errors.other_name) { %>
                                <div class="text-danger">
                                    <%= errors.other_name.msg %>
                                </div>
                            <% } %>
                        </div>

                        <div >
                            <label class="label-product"><b>Imagen del producto</b></label>
                        </div>
                        <div>
                            <input type="file" name="imagenProducto" id="imagenProducto"
                                class="form-control <%= locals.errors && errors.imagenProducto ? 'is-invalid' : null %>"
                                value="<%= locals.oldData ? oldData.imagenProducto : null %>">
                            <% if (locals.errors && errors.imagenProducto) { %>
                                <div class="text-danger">
                                    <%= errors.imagenProducto.msg %>
                                </div>
                            <% } %>
                        </div>

                        <div>
                            <label class="label-product" for="review"><b>Reseña del producto (4 to 150 characters):</b></label>
                        </div>
                        <div>
                            <input type="text" id="review" name="review"
                                class="form-control <%= locals.errors && errors.review ? 'is-invalid' : null %>"
                                value="<%= locals.oldData ? oldData.review : null %>">
                            <% if (locals.errors && errors.review) { %>
                                <div class="text-danger">
                                    <%= errors.review.msg %>
                                </div>
                            <% } %>
                        </div>
                        
                        <div>
                            <label class="label-product" for="description"><b>Descripción del producto (4 to 600 characters):</b></label>
                        </div>
                        <div>
                            <input type="text" id="description" name="description" class="form-control <%= locals.errors && errors.description ? 'is-invalid' : null %>"
                            value="<%= locals.oldData ? oldData.description : null %>">
                            <% if (locals.errors && errors.description) { %>
                                <div class="text-danger">
                                    <%= errors.description.msg %>
                                </div>
                            <% } %>
                            
                        </div>
                        <div>
                            <label class="label-product"><b>Maceta</b></label>
                        </div>
                        <div>
                            <select name="maceta" id="tipo" class="maceta <%= locals.errors && errors.maceta ? 'is-invalid' : null %>"
                                value="<%= locals.oldData ? oldData.maceta : null %>">
                            <% if (locals.errors && errors.maceta) { %>
                                <div class="text-danger">
                                    <%= errors.maceta.msg %>
                                </div>
                            <% } %>
                                <option value="1">Cerámica</option>
                                <option value="2">Plastico</option>
                                <option value="3">Plastico blando</option>
                            </select>
                        </div>

                        <div>
                            <label class="label-product" for="valor"><b>Valor $</b></label>
                        </div>
                        <div>
                            <input type="number" id="valor" name="valor" min="10" max="10000000"
                                class="form-control <%= locals.errors && errors.valor ? 'is-invalid' : null %>"
                                value="<%= locals.oldData ? oldData.valor : null %>">
                            <% if (locals.errors && errors.valor) { %>
                                <div class="text-danger">
                                    <%= errors.valor.msg %>
                                </div>
                            <% } %>
                        </div>
                        <div class="error">
                            <ul></ul>
                        </div>
                        <div class="button">
                            <input class="button-accept" type="submit" value="Aceptar">
                        </div>
                    </form>
                </div>
                <script src="/js/createProductForm.js" ></script>                
            </div>
        </main>
        <%- include('../partials/footer') %>
    </body>

</html>